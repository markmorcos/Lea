name: deploy-lea

on:
  workflow_dispatch: {}
  push:
    branches: [main]
  # Fired by the CMS on publish; rebuilds the site with the fresh content.
  repository_dispatch:
    types: [cms-publish]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger infrastructure deployment
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.INFRASTRUCTURE_PAT }}
          repository: markmorcos/infrastructure
          event-type: deploy-lea
          client-payload: |-
            {
              "repository": "markmorcos/Lea",
              "token": "${{ secrets.DEPLOYMENT_TOKEN }}",
              "version": "${{ github.sha }}",
              "config_file": "deployment.yaml"
            }
